# Warden Wallet — repo directives
- **Current pickup (2026-09-05):** the user authorized a separate devnet website/passkey test flow after the public-address-only slice. Read the new top handoff in `docs/NEXT-SESSION.md` and `docs/DEVNET-TEST.md`. Local code now implements passkey create, verified connect and native root transfer through a separate `build:devnet` output; production composition and pins remain unchanged. The checked-in program address is absent on devnet at slot 493700271, so real testing needs an owner-authorized deployment. Website publication, browser/full-gate verification and independent review remain UNVERIFIED. Exact focused command/SHA evidence belongs in `docs/CRITIQUE-2026-09-05.md`. The prior user instruction authorizes normal commits and pushing `origin/phase1b`.
- Spec: docs/superpowers/specs/2026-08-18-warden-wallet-design.md (**rev 9 = Phase 1B close-out 2026-08-22**; + binding 2026-08-19 UI/security erratum; §17 Assurance is binding process). Plans: docs/superpowers/plans/. Phase 1A measured costs + milestone review: docs/program/PHASE1A-MEASUREMENTS.md; Phase 1B measured costs + review close-out: docs/program/PHASE1B-MEASUREMENTS.md.
- Single pickup memo for the 2026-08-19 research campaign: docs/OVERNIGHT-HANDOFF-2026-08-19.md. Next-session operational handoff: docs/NEXT-SESSION.md. Client/release plan: docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md. Optional vanity-primary-account plan: docs/superpowers/plans/2026-08-19-warden-vanity-primary-account.md. Extension/mobile UI research and plan: docs/research/2026-08-19-wallet-ui-extension-mobile.md and docs/superpowers/plans/2026-08-19-warden-s-tier-ui-mobile.md. None changes the Phase 1B order; C0/V0 may seed honest unimplemented invariants at a clean boundary, UI U0–U2 may proceed only under an explicit Figma design lease, client/UI implementation waits for Phase 1B Task 8, and vanity UI integration additionally waits for the production extension trust boundary. Vanity grinds only a public salt for the Warden PDA; never reuse /opt/vanity-bot code, binaries, or private-key flow.
- Phase 1B plan: docs/superpowers/plans/2026-08-18-warden-phase1b-execute-swap.md (rev 3). Task run order 0 → 10 → 1 → 2b → 2 → 3 → 4 → 5 → 6 → 8 → 9, Task 11 in parallel; there is deliberately no Task 7 (renumbered to 2b).
- Governing execution order (2026-08-20): docs/superpowers/plans/2026-08-20-warden-research-adjudication-and-campaign-plan.md (joint Claude+Codex adjudication, SHIP-DOC). **A0 DONE** (11 Codex rounds; 5 RED-verified conservation fund-loss fixes; executable review lane + REVIEW-RUNS.jsonl). **C0+V0 DONE** (9 Codex docs rounds; 26 client/vanity/deploy invariants seeded; C1a/C2a/C4b/5E/Task-11R/Task-2-split written into source plans; 7-iteration deploy-gate hardening). Both close-outs in docs/program/PHASE1B-MEASUREMENTS.md. **Task 2 DONE** (2A 204a118 + 2B 4c8575a, 4 rounds → 0 findings). **Task 3 DONE** (a7b7824…300face, 7 review rounds WRDF-0034..0044 all adopted+fixed; round 7 confirmation = 0 findings at 300face): Registry + selector rule + registry_allows ((program,selector) authority-position role checks, fail-closed) + defaults + init_registry (upgrade-auth-gated) + grant_session allowlist (LOADS registry + requires `is_allocated_list`, WRDF-0044) + create_account registry (ceremony-bound) + canonical `encodeCreateBody` (215 B, WRDF-0042). WRD-SESS-07 inverted "must be 0" → "must resolve to an allocated list". **Task 4 DONE — code-complete** (d47d8ce…08a8b56, 5 sol@max rounds; engineering findings WRDF-0045..0049 converged): stage_open/chunk/finalize/close — Stage PDA `["stage", account, creator, hash]` (creator bound to close the ND-SQD3-LO-01 squat by construction, WRDF-0045), chunk upload, finalize (hash + generation/policy_version binding), close (creator/expiry, rent→creator); 23 integration tests; MEASURED stage_chunk cap = 977 B v0/client (979 legacy), `STAGE_CHUNK_PAYLOAD_CAP`; WRD-BUF-01/02 + WRD-STAGE-01 test-covered, WRD-STAGE-02 (consume) → Task 5. **RELEASE-BLOCKER (owner/counsel, blocks SHIP not code):** WRDF-0050 — the anti-squat fix uses a generic per-owner-PDA pattern but the prior-art corpus flags any non-MIT (Squads AGPL) lineage for counsel; carried UNRESOLVED, not fixed. **Task 5 (execute) code-complete** (`a7efe93`…`3632deb`, parts 1–5): payload layer (`parse_payload`/`classify_spl_token_op`/`resolve_payload`/`compute_accounts_hash`/`enforce_pda_writable`) + handler (`instructions/execute.rs`): session/root (XOR) × inline/staged (XOR); fixed deny-list on BOTH paths before registry; adapter registry on session path; conservation snapshot/compare around a real `invoke_signed` CPI loop; per-mint caps → `buckets::debit`; stage consume-once (WRD-STAGE-02). `OP_EXECUTE_ACTION=0x07` + TS `encodeExecuteBody` cross-language-pinned. 320 lib + 25 `tests/execute.rs` (real SPL+mutator CPIs) + 109 TS; clippy clean. Two design findings resolved: per-ix duplicate-index reject relaxed (vault-sweep close names the PDA twice); PDA-not-writable moved from the pure parser into `enforce_pda_writable` (handler, post-`deny_scan`) so the rule-4a vault-sweep close can credit the PDA while a writable PDA stays refused everywhere else. Errors 6056/6057. **Task 5 DONE 2026-08-21** (4 sol@max rounds, no new program findings by round 4; WRDF-0053 fixed, 0051 guard+claim-corrected, 0055 doc+test, 0056 narrowed, 0057 gate evidence; standing deferred: 0051-nested→Task 6 preflight, 0052→Task 5E events, 0054→Task-3 hygiene). **Sweep verdict: execute caps are HEAP-bound, not CU/byte — MEASURED `MAX_EXECUTE_ACCOUNTS_TOTAL=24` / `MAX_EXECUTE_WRITABLE=20`** (22-writable OK / 24 OOM on the default 32 KiB heap; `RequestHeapFrame` inert under Anchor's capped allocator; worst CU 83.8k; v0+ALT ≤ 486 B), boundary-tested on-chain. **Task 6 prereq DONE** (@7936419 custom-heap allocator, caps 24/20→32/28). **Task 6 (swap) DONE @949c1f5** (Jupiter v6-pinned adapter; 4 sol@max rounds → 0 program findings by round 4). Native swaps rejected in 1B (CRITICAL WRDF-0061); realized fund-loss bound stacked 4-deep — route_hash root-binding + treasury-fee-taken + writable-vault pinning + canonical-ATA pinning — over net conservation + caps; pre-CPI cap/lifetime/bucket probe; min_out=net out-gain; OP_SWAP_ACTION=0x08 + TS encodeSwapBody; errors 6060-6074; test-jup-mock at real v6 positions; IDL pinned 764ea6d7…; CI publishes a clean default-profile prod .so + pin attestation. **Deferred (not 1B defects): byte-exact route_plan parse before mainnet (0031/0059); events→Task 5E (0052).** **Task 8 (TS client) DONE @ef69e23 close-out** (9 sol@max rounds → round 9 = 0 findings; SDK product code converged @round 3, rounds 4–8 hardened the review ledger itself). `payload.ts` codec + `wrapForExecute` (build→compile→decompile the outer ix, derive accounts_hash+caps from runtime-coalesced effective metas; fail-closed rejects; payload⊆outer subset guard; CU/heap-frame normalization + validation; client caps) + cross-language golden fixtures (read-only vectors + gate drift guard). WRD-EXEC-11/12 test-covered. Gate green @d73118f. Deferred (non-blocking): typed vault-sweep close builder, on-chain relayer/alias execute integration. **Task 11 DONE 2026-08-22** — WRD-DEP-01 (deploy-gate governance + hash) converged at round 16 (Codex UPHELD); WRD-DEP-02 (deploy check 3: adapter-`Registry` authentication + complete-config diff, every selector re-derived from source and authenticated against the hash-pinned Jupiter v6 IDL) implemented and converged in 8 sol@max rounds. **Task 9 DONE 2026-08-22** — spec bumped to **rev 9** (Phase 1B close-out), Task-11 close-out recorded in PHASE1B-MEASUREMENTS.md. **Phase 1B PROGRAM WORK COMPLETE** — 283 `@warden/core` tests; whole `phase1b` branch pushed. Landing page shipped LIVE at drinkerlabs.info/warden/ (dark brushed-steel shield brand; dual Claude-visual + Codex-source critique then improved; v1 backed up to index.html.bak-*). Honest residuals for a release candidate in PHASE1B-MEASUREMENTS.md + docs/security/ (live-cluster solana-verify parity, real-Anchor-writer golden Registry vector, real mainnet governance/treasury pins, external audit, shipped client/extension). **Open OWNER items (block SHIP, not code):** C1a (create_account vs pinned production origin + a freeze-vs-migration decision before public account creation), external security audit, on-chain deploy, and counsel sign-off on WRDF-0050 (Squads AGPL lineage) + WRDF-0089 (Squads reader) + the Jupiter-IDL licensing note in THIRD_PARTY_NOTICES.md. **Client track (post-1B) STARTED: C2a DONE 2026-08-22** — `assertionToCompact()` strict-DER-parse + mandatory low-S normalization in `packages/core/src/webauthn/assertion.ts` (+ 13 tests, converged through Codex sol@max round 4 = 0 findings @`3f56914`); proven on the RECORDED real high-S Chrome assertion (normalizes to expected bytes; precompile-equivalent lowS verifier rejects raw-high-S / accepts normalized). **WRD-SIG-01 deliberately stays `unimplemented`** (WRDF-0103): binding acceptance needs the recorded assertion through the REAL on-chain secp256r1 precompile end-to-end via the C3 production ceremony builder (unbuilt) — the Noble check is a proxy, not the precompile. **GROK exploit-audit remediation DONE 2026-08-22** — independent adversarial pass (docs/security/GROK-EXPLOIT-AUDIT-2026-08-22.md) patched: EXP-01 realized 85-bps swap-fee floor; EXP-02 MintTo/MintToChecked/Freeze/Thaw deny-listed + conservation freezes vault-controlled mint `supply`; EXP-03/-05 writable Stake/Vote/ProgramData rejected pre-CPI + session `swap` refuses the `route` variant (shared_accounts_route only; err 6075) + optional route[4] dest pinned; EXP-04 empty payload rejected; EXP-06 direct reincarnation (ATA-create/InitializeAccount* of a BEFORE vault token) deny-listed, nested-both-halves residual documented (fee-payer-only, no vault net-loss); EXP-07 `encodeGrantBody` + `OPS_MASK_*` added, layout comments fixed. 17 GROK regressions (red-at-9a427aa → green) + inverted repro fixtures; full gate green; details in docs/program/PHASE1B-MEASUREMENTS.md. **The GROK fix range is NOW genuinely Codex-reviewed (2026-08-23, WRDF-0107 corrected an earlier claim that ran ahead of the evidence):** the round owed since the 2026-08-22 content-filter block ran clean over `9a427aa..0039681` — `scripts/review.sh`, gpt-5.6-sol@max, 67 seeded invariants, thread `phase1b/grok-remediation/round-1`, recorded in REVIEW-RUNS.jsonl. It returned **4 findings, all adjudicated ACCEPTED**: WRDF-0104 (important — UNSUPPORTED_WRITABLE_OWNERS misses System-owned durable-nonce and Loader-v4 accounts, so root `execute` can move value unmetered; LZR-ACC-C2 fail-open class), WRDF-0105 (important — `deny_scan` keys on the DIRECT payload program only, so a forwarding CPI can carry the PDA's propagated signer into FreezeAccount/nested MintTo→Burn on a vault-controlled mint; SWIG-ACC-C2 class), WRDF-0106 (minor — the out-mint platform-fee floor uses NET output as its basis where Jupiter's `outAmount` is already net of the fee, so the 85 bps floor under-charges), WRDF-0107 (minor — this very ledger contradiction plus green-gate claims that named no command or SHA). **B4 DONE 2026-08-23 as a narrow, per-invariant-verified pass** (never a batch flip): an evidence-mapped audit of the 9 stale rows found 3 already fully covered (WRD-EXEC-01/05/08 — the earlier note that WRD-EXEC-08 had no covering test was WRONG; `runtime_program_owner_change_is_rejected` is exactly its SWIG-ACC-C1 case), 5 closable by named missing tests (WRD-EXEC-02 multi-ATA coalescing; WRD-DENY-01 never exercised Token-2022; WRD-DENY-02 zero coverage; WRD-STAGE-02 no stale generation/policy_version test; WRD-CAP-09 no bucket-debit assertion and no ROOT over-threshold negative), and 1 genuinely unpromotable (WRD-FRZ-03 — compound over the unbuilt `execute_pending`; its swap half had NO frozen test at all, now added). 8 rows promoted to `test-covered` with per-test evidence + honest caveats in `notes`; ledger 48→56 test-covered, 39→31 unimplemented. **WRDF-0105 took THREE rounds to close and the fix was wrong twice** — round 1 gated only the direct payload program; round 2 covered 2 of 4 mint-authority roles (missing `transfer_fee_config_authority` / `withdraw_withheld_authority`, reachable via T2022 `WithdrawWithheldTokensFromAccounts` tag 26); round 3 found `PermanentDelegate` was a FIFTH role whose pubkey `snapshot.rs` extracted then **discarded**, and the danger-mint check only fired on WRITABLE mints while `TransferChecked` takes its mint read-only — a **PROVEN** drain (third-party T2022 account 9_000→0 through a real CPI, zero outflow, no bucket debit). The round-2 miss was the ADJUDICATOR's: Codex's round-2 text named PermanentDelegate and the main loop dropped it from the worker brief. **WRDF-0108 caught a false promotion by the main loop**: WRD-CAP-09 was promoted while its statement certifies the unbuilt `execute_pending` — root cause, coverage verified against a TRUNCATED reading of the statement; resolved by splitting into WRD-CAP-09 (1B, keeps evidence) + **WRD-CAP-10** (1C, unimplemented). **CLASS FIX @89bfac2**: two independent reviewers (Grok + Codex MCP) showed the remediations closed the INSTANCE three times and never the CLASS — `scan_extensions` extracts authority pubkeys for only 2 of 17 T2022 roles, and `execute`'s gate consulted neither `has_unrecognized_ext` nor `dangerous_ext`. `execute` now also fails closed on `UNMODELED_AUTHORITY_DANGERS = DANGER_TRANSFER_HOOK | DANGER_CONFIDENTIAL` and `has_unrecognized_ext` (err **6077**), preserving the deliberate stranger-held-mint narrowness. WRD-EXEC-09 is now HALF-true (transfer_hook + confidential unconditional; permanent_delegate + transfer_fee deliberately not) and stays `unimplemented`. Gate green @89bfac2: **674 rust / 0 failed**, 301 core. **PROVIDER-INDEPENDENT ASSURANCE**: `scripts/review.sh` blocked 3× by the OpenAI cyber content filter on our own drain-proving regressions, so **`scripts/review-grok.sh`** now makes xAI `grok-4.3` a RECORDED reviewer via the same expectations/validator/append machinery (`effort: null`, `reviewer_model` from the API not self-report, prompt saved as `<round>.prompt.txt` because it IS the evidence base). **Round 101 recorded** — but its attempt 1 was correctly REJECTED (Grok refused to rule on elided context; anti-silence validator caught it; ledger held), and round 101 carries an explicit calibration caveat: **materially shallower than Codex and it ruled WRD-EXEC-09 `not_applicable` when this very range changed it. It is a FALLBACK, not an equivalent. That Codex debt was later discharged by the recorded R0 campaign threads in `docs/NEXT-SESSION.md`.** **C2 keyring core DONE** (`packages/core/src/keyring/`, core 301→**377** tests): versioned AES-256-GCM envelope, injective length-prefixed AAD (injectivity EXHIBITED by a total left inverse; the test first demonstrates the naive-concat collision), cross-cluster replay rejects (WRDF-0023), Argon2id pinned to the **RFC 9106 §5.3 published vector**. All three WRD-KEY rows stay `unimplemented` — nothing produces/consumes envelopes yet (C1). **Two honest C2 gaps AT THE TIME (both since closed — Fable audit K-8, 2026-09-02): the "one envelope unlockable by either path" KEK/DEK layer is now BUILT (`packages/core/src/keyring/bundle.ts` + record v2 in `record.ts`: one random DEK wraps the payload, wrapped independently by the password KEK and the optional WebAuthn-PRF KEK); the Argon2id params are exercised by the gated `bench:argon2` lane. K-1 (2026-09-02): the extension now pins cluster genesis + program id at record adoption (`apps/extension/src/background/expected-keyring-context.ts`, WRD-EXT-03) — the `solana:mainnet` pin is an OWNER release decision to confirm.** **Fable → Warden follow-up COMPLETE 2026-09-03:** R0–R8 are recorded; WRDF-0112..0161 comprise 50 adopted findings, with 49 remediated and verified and WRDF-0121 intentionally unverified pending owner decision O8. The exact gate command and its passed-at SHA `23df466e2c31a0ac5f21b4c552abff26a7f540f3` are recorded in `docs/NEXT-SESSION.md`; the docs-only close is `ede734d608b681f8478062da2f8eba1d33dd6dbd` and does not inherit that verdict. The original Codex handover is historical evidence, not the next queue.** Later candidates: C1 (MV3 extension trust boundary — large greenfield, owner-visibility flagged).
- **C2 status correction (WRDF-0114):** only the former KEK/DEK construction gap is closed. The Argon2id production-floor gap remains OPEN: `bench:argon2` exercises the provisional profile and its responsiveness/cancellation behavior on this host, but does not supply the binding slowest-supported-desktop measurement, production-floor selection, or spec amendment. Extension code now persists and adopts envelopes; the remaining WRD-KEY-02/04 residuals in `invariants.jsonl`, not the older “nothing produces/consumes envelopes” sentence, are authoritative.
- Assurance (spec §17) is binding: every Codex review is seeded with docs/security/INVARIANTS.md + docs/security/PRIOR-ART-FINDINGS.md and the named sibling files (transcript.rs, state/session.rs, errors.rs, buckets.rs); output is JSON validated against .codex/schemas/warden-findings.json; truth_status and evidence_type are separate axes; silence on a seeded invariant is a FAIL. Review profile lives at $CODEX_HOME/warden-review.config.toml, not in-repo.
- Payload account indices are LOGICAL, one canonical mapping: logical[0]=smart_account, logical[1]=signer, logical[2+k]=remaining_accounts[k]. Never index the raw physical account slice; accounts_hash is over the logical list.
- Published task/review ledgers: docs/spikes/PHASE0-LEDGER.md (Phase 0) and docs/spikes/PHASE1A-LEDGER.md (Phase 1A) — sanitized copies of the working SDD ledgers.
- Never import from spikes/ into packages/ or apps/. Spikes are throwaway evidence.
- Heavy Rust builds are serialized on this host; never run two cargo builds at once.
- Amount math in Rust: checked_* only. TS: bigint for lamports/token amounts.
- Design tokens come from Figma (docs/design/figma.md) → packages/ui-tokens; do not hand-edit tokens.css. Current Figma corrections are binding before implementation: partial first/last-address matching is attention friction, never identity verification; dust/lookalike override requires fresh trusted-source entry plus full-address comparison and authentication; light-mode warning indicators must measure at least 3:1 against every allowed background. The existing matched/dust Figma frames are legacy evidence until replaced.
- Approval UI keeps origin, decode, simulation, and policy as independent evidence axes—never one green “safe” verdict. High-consequence approval belongs in a request-bound approval window, not solely in the 360 × 600 popup. Native mobile credentials, verified links, MWA support, and capture privacy remain UNVERIFIED until real-device spikes.
- Two-model loop: every task is implemented by an isolated worker and adversarially reviewed by Codex (gpt-5.6-sol @ max) before it counts as done; milestones get a security review; pre-deploy diff gets a recon.
- Public repo: never commit keypairs, .env, or anything under .superpowers/; program keypair policy in docs/PROGRAM-KEYS.md.
