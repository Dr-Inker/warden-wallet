{
  "name": "@warden/extension",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@warden/core": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "vitest": "^3.0.0"
  }
}
