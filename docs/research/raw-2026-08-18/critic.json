{
  "contradictions": [
    "Existence of `execute`/adapter-registry/conservation checks: A1, A2, A5, B2, B4, B5 all analyze them as shipped code and hang their top recommendations on them; B7 says Phase 1B is planned-not-built. VERIFIED (GitHub, 2026-08-18): programs/warden/src/instructions contains only create_account, freeze, unfreeze, grant_session, revoke_session, rotate_nonce, transfer, mod. Six reports' headline advice targets code that does not exist, so it is design input, not audit findings.",
    "LazorKit audit status: A4 says 'no named third-party auditor found \u2014 treat as UNVERIFIED'; B2 and B5 cite a specific Accretion/Solana-Foundation PDF with 14 findings. VERIFIED via gh API: `audits/2026-accretion-solana-foundation-lazorkit-audit-A26SFR1.pdf` exists on main. A4 is wrong; B5's path (`docs/audits/`) is also wrong (it is `audits/`).",
    "Squads passkey support: A4 says passkey internals are unshipped/not public; A5 asserts the Squads Smart Account Program 'ships session keys/passkeys/direct debits'; B2 read the v5 repo and describes only sync execute paths, no passkey. Unresolved \u2014 matters because A5's recommendation 7 (model guardians on Squads primitives) assumes a maturity nobody confirmed.",
    "Test-harness trust: A2 treats the 292 LiteSVM tests as a sound base to layer proptest/Trident/Certora onto; B6 shows LiteSVM installs stub precompile accounts and never calls verify_precompiles. VERIFIED against litesvm master (sigverify=false by default, `precompiles` feature only loads programs). If Warden's suite is default-configured, its passkey/introspection tests can pass against a forged signature claim \u2014 which invalidates A2's ordering of investments.",
    "Framework assumption: A1/A2/B5 give Anchor-specific advice (Anchor >=0.30 close-account semantics, `require_keys_eq!`, Anchor account constraints) while A2 also says zero-copy and B2 notes the closest analogues (Swig, LazorKit) are Pinocchio. Warden's actual framework/Anchor version is never established, so an unknown fraction of that advice is inapplicable.",
    "CPI depth: A1 states SIMD-0268 'will raise it to 8 on Agave v3+' as near-term planning input; B6 queried mainnet RPC and found the feature account null (not even queued). B6 is the verifiable claim; A1's framing invites premature dependence.",
    "Third-party simulation: A7 recommends integrating Blowfish/Blockaid as a second opinion; B3 documents Blowfish silently missing an `assign` ownership-transfer instruction (the exact Solana-specific drain class), and B1 shows Backpack depending on it. The recommendation and the disconfirming evidence are never reconciled."
  ],
  "suspect_claims": [
    "A3's cross-model asymmetry (Claude-reviews-Codex +18.1pp vs Codex-reviews-Claude -8.6pp, arXiv 2607.21656) is the load-bearing basis for adding a Claude reviewer, but comes from isolated Python LiveCodeBench tasks; could not verify (search budget exhausted). Read literally it says Warden's current Codex-only-reviews-Claude loop is net-harmful \u2014 too strong a conclusion for one narrow benchmark.",
    "A2's 'Warden is public on GitHub, which likely qualifies it for Certora's OSS tier' is sourced to a CVL rules doc, not a pricing page. A whole adoption sequence is staged on it.",
    "A5's recommendation to switch buckets to Swig-style epoch-floor resets gives no security rationale. Epoch-aligned windows arguably worsen boundary double-spend (full cap at end of window N, full cap at start of N+1); the claim that it is 'closer to non-gameable' than rolling is asserted, not argued.",
    "A1 simultaneously reports that Anchor >=0.30 removed the closed-account discriminator issue and recommends adding CLOSED_ACCOUNT_DISCRIMINATOR handling \u2014 one of the two is wasted work depending on the (unestablished) Anchor version.",
    "B2's `owner_seed` front-running claim is self-flagged as pattern-matched from TOB-SQUADS-7 without reading Warden's create_account tests; B7 independently asserts creation is unauthenticated. Neither established whether the PDA seed set actually permits a distinct attacker-controlled root, which decides whether this is critical or moot.",
    "A6 describes Warden's guardian/freeze/48h-contest design as 'structurally stronger than Argent/Loopring' \u2014 but per B7 Phase 1C is not started, so this compares a shipped system to a spec.",
    "B6 found the upstream brief referenced a SIMD-0579/keccak-p1600 that does not exist. That implies at least one input to this whole research round contained fabricated protocol facts; nobody traced which other briefed 'facts' share that provenance.",
    "Self-flagged but still cited downstream: the $713M Chainalysis figure (A7), Chrome 147 PRF (A7), the May-2025 $1.5M Phantom blind-signing incident and Murphy v. Phantom memory-resident-key allegation (B3), and dollar figures from the AI-compiled haveibeendrained doc (B7)."
  ],
  "unanswered": [
    "Codex process (1): No report defines what Codex reviews *against*. There is no invariant ledger, no spec-conformance corpus, no merge-blocking rule, no adjudication procedure for a disputed finding, and no measurement of Codex's false-positive rate on Warden's own repo. A3 proposes artifacts but no bootstrapping order.",
    "Codex process (2): Nobody proposes seeding both models with the concrete prior-art finding corpus (ACC-C1/C2/H1/H2/M1/M2, TOB-SQUADS-7/8, ND-SQD3-LO-01, Argent zero-guardian, Swig ACC-C1 owner-check) as a mandatory per-review checklist \u2014 the single cheapest way to counter shared LLM blind spots on Solana-specific classes.",
    "Codex process (3): A3 requires PoC-backed findings (PoCo-style), but B6 shows the available in-process harness cannot verify precompiles \u2014 so the substrate for reproducing passkey/introspection bugs does not exist. The dependency is never noticed.",
    "Codex process (4): Unaddressed uses of a second model that fit Warden better than free-form review \u2014 independent re-implementation of the clientDataJSON parser and cap arithmetic for differential testing, mutation-survivor triage, and adversarial test-vector generation for the WebAuthn scanner.",
    "OSS prior art (1): No file-level 'port this' list with licensing analysis. Swig is AGPL-3.0 and Backpack GPL-3.0; whether Warden can legally vendor any of it (and what that obligates for the extension) is never asked, despite multiple recommendations to copy their patterns.",
    "OSS prior art (2): No decision criteria for replay design \u2014 LazorKit's monotonic counter + MAX_SLOT_AGE=150 vs Swig's 60-slot window vs Warden's consumed-nonce set. A4 raises the tradeoff (resync after dropped/reordered tx) and drops it.",
    "OSS prior art (3): Nobody asks what prior art deliberately *avoids*. No comparable program implements Warden's before/after conservation check as the primary control; LazorKit instead uses gross-outflow caps plus authority snapshots. Whether balance-diffing is a novel, unvalidated bet is the key unexamined design question.",
    "OSS prior art (4): No verdict on the permission model itself \u2014 Swig's 26-variant RBAC and destination-scoped limits vs Warden's cap/bucket model \u2014 and which test-suite structure to copy (LazorKit/Swig Rust negative tests vs Squads' TS-only validator suite)."
  ],
  "followups": [
    "Determine empirically whether Warden's LiteSVM suite verifies secp256r1 signatures at all: check sigverify and precompile feature configuration, then attempt a forged Instructions-sysvar entry against root_verify. Specify the minimal harness (validator or configured LiteSVM) that makes introspection tests meaningful, and a CI gate that fails if passkey tests run unverified.",
    "Design a Codex review protocol built on a machine-checkable invariant ledger: derive invariant IDs from Warden's spec, seed every review with the named prior-art findings (ACC-C1/C2/H1/H2/M1/M2, TOB-SQUADS-7/8, ND-SQD3-LO-01), require schema-JSON output plus a reproducing test, and define adjudication and false-positive measurement.",
    "Run a controlled A/B on Warden's own Rust diffs: Claude-reviews-Codex versus Codex-reviews-Claude versus both, scoring true findings, false positives, and regressions introduced. The cited +18.1/-8.6pp asymmetry derives from isolated Python tasks; establish whether it holds for Anchor/Pinocchio at repo scale before restructuring the loop.",
    "Audit licensing and portability of prior art Warden may copy: Swig (AGPL-3.0), Backpack (GPL-3.0), LazorKit program-v2, Squads v4/v5. Determine what can be vendored, what must be reimplemented clean-room, and what obligations attach to a browser extension plus on-chain program distributed together.",
    "Adjudicate Warden's replay design: consumed-nonce set versus LazorKit's monotonic counter plus 150-slot freshness versus Swig's 60-slot window. Analyze dropped, reordered, and concurrent transactions, resync UX, storage growth and rent, and cross-device passkey usage. Produce a decision with failure-mode tests.",
    "Specify the unbuilt execute/conservation-check design before implementation: multi-mint and lamport coverage, post-CPI program-owner and token-authority snapshot re-verification, gross versus net per-transaction caps, Token-2022 extension policy (permanent_delegate, transfer_hook, transfer_fee, confidential), and a program-level deny-list for SetAuthority, Approve, and CloseAccount."
  ]
}