# Warden — invariant ledger (assurance layer L1)

**`docs/security/invariants.jsonl` is the source of truth. This file is generated from it.**
Edit the JSONL, then run `node scripts/gen-invariants.mjs` (or `--check` in CI). Never hand-edit
the table below the marker.

The ledger proves nothing by itself. What it buys is **addressability**: a review verdict becomes
"Codex ruled on `WRD-EXEC-07`" instead of "Codex reviewed the diff", and a gap becomes a row with a
status instead of an absence nobody notices. The shape is copied from Certora's Squads v4 report,
which publishes named CVL properties each with a status and a link to its machine-generated rule
report — **ID → statement → spec ref → code ref → prior art → status → evidence**, not prose.

Binding process: **spec §17** (`docs/superpowers/specs/2026-08-18-warden-wallet-design.md`).
Derivation and sources: `docs/research/2026-08-18-security-assurance-and-wallet-landscape.md` §1.

---

## 1. Record shape

One JSON object per line. Fields:

| Field | Type | Meaning |
|---|---|---|
| `id` | string | `WRD-<NAMESPACE>-<NN>`. Immutable once published; never renumber, retire instead. |
| `title` | string | Short label, for tables and review prompts. |
| `statement` | string | **One testable sentence.** If you cannot imagine the negative test, the statement is too vague. |
| `spec_ref` | string | Path + section in the design spec. The ledger never invents policy; it gives existing spec text an ID. |
| `code_ref` | string \| null | `path` or `path:line` where the invariant is enforced. `null` until implemented. |
| `phase` | `1A` \| `1B` \| `1C` | Which phase owns the surface. |
| `prior_art` | string[] | IDs from `PRIOR-ART-FINDINGS.md`. |
| `status` | enum | The invariant's own standing — see §2. |
| `evidence` | object[] | `{type, path, name?, sha}`. `type` is the evidence-type enum (§3). `sha` is the commit at which the evidence was last verified. |
| `last_reviewed` | object | `{thread, date}` — which review thread last ruled on this row. |
| `notes` | string | Stated limits, open gaps, the reason a status is being held down, measured numbers. |

## 2. `status` — the invariant's own axis

**Ascending proof strength. Exactly one field with these values. There is no `holds: true|false`
boolean alongside it** — a second representation of the same fact is a desync waiting to happen.

| Status | Means |
|---|---|
| `unimplemented` | The surface does not exist yet. The honest default for every 1B/1C row. |
| `llm-asserted` | A model (or a human reading the code) believes it holds. **Not evidence.** |
| `test-covered` | At least one executable test in this repo fails if the invariant is violated. |
| `mutation-tested` | The covering tests survive mutation of the enforcing code (`cargo-mutants`, L4). |
| `proven` | A formal proof exists (Certora/CVLR, L6). **The `notes` field must state the proof's limit**: a CVLR proof says nothing about CPI effects, precompile semantics, account loading, or the SBF the validator runs. |
| `holds` | ≥1 accepted evidence artefact of an allowed type **and** no open contradicting finding. The ledger-side counterpart of a `REFUTED` attack claim. |

**`holds` is the row L3 samples for false negatives** — 1-in-10 monthly, blind re-review, weighting a
missed bug worse than a false alarm. That is the entire reason `holds` is a distinct value and not a
synonym for `test-covered`.

**Who may set `holds`:**

1. An **evidence artefact** of an allowed type (§3), recorded with a path and a SHA; **or**
2. an explicit **human security adjudication**, recorded in `REVIEW-SCORECARD.jsonl` with its rationale.

**Model agreement is a confidence signal, never evidence, and never promotes a row on its own.**
Two models can be confidently wrong together — that is precisely why prior-art seeding exists.
Agreement raises triage priority and nothing else. **No tool confers a proof status until it has been
piloted on this repo**: a clean fuzzing campaign raises an invariant to `test-covered` at best, never
to `proven`.

**Seeding rule: nothing is seeded at `holds`.** Every row starts at its honest status — mostly
`unimplemented` for 1B surfaces and `test-covered` for shipped 1A ones.

## 3. `evidence[].type` — how it is known

`red_test` · `static_trace` · `formal_counterexample` · `config_attestation` · `primary_source`

Same enum as a finding's `evidence_type` (§4), deliberately: the two sides of the ledger have to speak
one vocabulary or a finding can never be matched to the invariant it contradicts.

On the ledger side `red_test` means **an executable test artefact in this repo that fails if the
invariant is violated** — the negative/attack tests, not the happy-path ones. A test that only asserts
the happy path is not evidence for an invariant; it is evidence the feature works.

## 4. How a finding relates to a row

A finding lives in `.codex/schemas/warden-findings.json` and carries **two orthogonal axes that must
never be collapsed**:

- **`truth_status` ∈ `POTENTIAL | CONFIRMED | REFUTED`** — the claim's standing.
- **`evidence_type`** — *how* it is known.

Collapsing them ("no red test ⇒ inconclusive") would discard static traces, supply-chain and
dependency-provenance findings, key-management and release-credential failures, upgrade-authority
misconfiguration, economic/MEV and liveness attacks, and formal counterexamples — all real, most
unreproducible as a LiteSVM test.

**Reproducers.** Required only where one is *feasible*. When present it must **FAIL on `base_sha` and
PASS on `fixed_sha`** — "fails on HEAD" is the wrong predicate the moment a fix lands. Where a
reproducer is infeasible, the finding routes to **human security adjudication** and is recorded with
the reason. It is **never auto-downgraded**.

**Adjudication rules (spec §17, restated because they bind here):**

1. A finding raised by **either** model enters as `POTENTIAL`.
2. Model agreement is confidence, not evidence.
3. `CONFIRMED` requires an evidence artefact of an allowed type **or** a recorded human adjudication.
4. **Disagreement escalates to a human and is never auto-dismissed**; the ruling is recorded.
5. **Any diff that moves an accept/reject boundary gets a mandatory second-round confirm.** Precedent:
   the round-1 fix to the P-256 root check over-corrected and rejected `x = 0` — a valid point — and a
   *second* Codex round caught it (`4b409f7`).
6. **Silence on a seeded invariant is a FAIL, not a pass.**

A `CONFIRMED` finding against a row **immediately** knocks that row down to `llm-asserted` (or lower)
and it stays there until the fix lands *with* a test. A row may not sit at `holds` with an open
contradicting finding, by definition.

## 5. How Codex audits the ledger, per milestone

`scripts/review.sh <base-sha>` is the mechanism; the rules it enforces:

1. **Clean tree is a precondition, and the checked-out HEAD must BE the head of the range.** A dirty
   tree makes codex repeat *hallucinated* findings unrelated to the diff
   ([openai/codex#8404](https://github.com/openai/codex/issues/8404)), and a worktree that does not
   match the range under review means the model reads files that are not in it.
2. **Seed the prompt with** the invariant rows overlapping the diff, `docs/security/PRIOR-ART-FINDINGS.md`
   (mandatory, every round), and the **named sibling files carrying cross-cutting invariants** —
   `programs/warden/src/root_verify/transcript.rs`, `programs/warden/src/state/session.rs`,
   `programs/warden/src/errors.rs`, `programs/warden/src/buckets.rs`.
   The one false positive in the 1A pilot came from **reviewer scope, not model identity**: Codex flagged
   `cluster_tag` binding in `create_account.rs`, a property `transcript.rs:69-76` documents as knowingly
   deferred, in a file Codex had not been shown.
3. **Blind and anti-rewrite.** Diff + spec section + seeded invariants; no author framing; an explicit
   instruction against rewriting working code.
4. **Standing checklist item for dimensional/scaling bugs.** Moonwell shipped Claude-authored code using
   a cbETH/ETH ratio as if USD-denominated — 2,000× underpricing, $1.78M, past a human reviewer.
5. **Output is JSON validated by an independent validator**, never trusted because `--output-schema` was
   passed (that flag constrains the *final response*, not every JSONL event).
6. **Silence on a seeded invariant is a FAIL** — and the seed list the verdicts are checked against
   comes from the **wrapper**, not from the model's own report of what it was asked. `review.sh`
   computes the list from this ledger, writes it to an expectations file *before* the model runs, and
   `validate-findings.mjs` requires exact set equality against it. Checked against the model's
   self-reported `seeded_invariants` alone, the rule is satisfiable by shortening the list, which
   makes it worthless — the negative tests in `packages/core/test/security-ledger.test.ts` pin exactly
   that failure.
7. **Every finding is logged**, including disputed and scoped-out ones, with adjudication rationale.
   Four Codex rounds on Phase 1A produced 1 Critical / 4 Important / 6 Minor at 100% adopted — that is a
   survivorship artifact of what gets committed, **not** a 0% false-positive rate.

**Every task in the Phase 1B plan updates this ledger in its own commit** — new invariants at their
honest status, evidence paths filled in as tests land, `last_reviewed` stamped. **A task is not done
while its invariants are still `llm-asserted`.**

## 6. Namespaces

| Namespace | Surface | Seeded |
|---|---|---|
| `WRD-ROOT-*` | secp256r1 / WebAuthn root verification, proof of possession, precompile binding | yes |
| `WRD-NONCE-*` | root nonce consumption and freshness | yes |
| `WRD-CAP-*` | per-tx / day / rolling-30-day / lifetime caps and bucket arithmetic | yes |
| `WRD-EXEC-*` | `execute`/`swap` conservation, snapshots, value accounting | yes (all `unimplemented`) |
| `WRD-FRZ-*` | freeze gating | yes |
| `WRD-DENY-*` | the fixed deny-list above the registry | yes (all `unimplemented`) |
| `WRD-BUF-*` | buffer/`Stage` PDA squatting **and** the 1232-byte packet budget | yes |
| `WRD-STAGE-*` | staged-content binding and consumption | yes (all `unimplemented`) |
| `WRD-SESS-*` | session grant/revoke lifecycle: ops-mask, ceiling, expiry, generation, prior-authority binding, allowlist id, grant shape | yes |
| `WRD-CD-*` | clientDataJSON parsing, if it outgrows `WRD-ROOT-06` | reserved |
| `WRD-REG-*` | adapter registry (Task 3) | reserved |
| `WRD-TL-*` / `WRD-GRD-*` | timelock / guardians (Phase 1C) | reserved |

`WRD-ROOT-01` is referred to as `WRD-PoP-01` in some plan prose; `WRD-ROOT-01` is canonical.

## 7. `REVIEW-SCORECARD.jsonl` (L3)

`docs/security/REVIEW-SCORECARD.jsonl` starts **empty**; `scripts/review.sh` appends to it. One JSON
object per line — a flattened finding plus its adjudication, so the file answers the question the
current loop structurally cannot: **how often is the reviewer wrong?**

```json
{"finding_id":"WRDF-0001","thread":"phase1b/task-05/round-2","date":"2026-08-19",
 "reviewer_model":"gpt-5.6-sol@max","base_sha":"…","severity":"critical",
 "truth_status":"CONFIRMED","evidence_type":"red_test","invariant_ids":["WRD-EXEC-06"],
 "ruling":"adopted","ruled_by":"human:owner","rationale":"…","reproducer_verified":true}
```

**Log every finding — disputed and scoped-out ones included.** Four Codex rounds on Phase 1A produced
1 Critical / 4 Important / 6 Minor at **100% adopted**; that is a survivorship artifact of what gets
committed, not a 0% false-positive rate, and a scorecard that only records adopted findings would
reproduce the artifact forever.

**Monthly:** blind re-review a **1-in-10 sample of rows sitting at `holds`** — that is where a false
negative hides — weighting a missed bug worse than a false alarm.

---

<!-- BEGIN GENERATED: scripts/gen-invariants.mjs -->

_Generated by `scripts/gen-invariants.mjs` from `docs/security/invariants.jsonl` — 88 invariants. Do not edit this table by hand._

| status | count |
|---|---|
| `unimplemented` | 40 |
| `llm-asserted` | 1 |
| `test-covered` | 47 |
| `mutation-tested` | 0 |
| `proven` | 0 |
| `holds` | 0 |

### Phase 1A

| ID | Invariant | Status | Spec | Prior art | Evidence |
|---|---|---|---|---|---|
| `WRD-NONCE-01` | **Root nonce is a strictly increasing odometer** — SmartAccount.root_nonce increases by exactly 1 on every successful root instruction and never decreases, so no assertion is consumable twice. | `test-covered` | design.md#4 (root nonce = an odometer, not a set) | `LZR-ACC-M1` | `programs/warden/tests/root_verify.rs` :: `rotate_nonce_ok_and_nonce_increments`<br>`programs/warden/tests/root_verify.rs` :: `consecutive_ceremonies_each_consume_one_nonce` |
| `WRD-NONCE-02` | **Strict n+1 equality, not a window** — A transcript whose root_nonce is not exactly equal to the stored value is rejected; there is no acceptance window and no replay bitmap. | `test-covered` | design.md#4 | `LZR-ACC-M1` | `programs/warden/tests/root_verify.rs` :: `replay_same_assertion_rejected`<br>`programs/warden/tests/root_verify.rs` :: `stale_nonce_far_in_the_past_rejected_as_challenge_mismatch` |
| `WRD-NONCE-04` | **Wall-clock expiry is the secondary bound** — expiry_ts must satisfy now <= expiry_ts <= now + 600s; an expired transcript and one expiring beyond the 600s horizon are both rejected, and the 600s boundary itself is accepted. | `test-covered` | design.md#4 | — | `programs/warden/tests/root_verify.rs` :: `expired_rejected`<br>`programs/warden/tests/root_verify.rs` :: `future_expiry_beyond_600s_rejected`<br>`programs/warden/tests/root_verify.rs` :: `expiry_exactly_at_the_600s_boundary_accepted` |
| `WRD-CAP-01` | **Cap dominance** — Every accepted spend satisfies amount <= min(per_tx, per_day - spent_today, per_30d - sum(ring)), and for a session additionally lifetime_spent + amount <= lifetime_cap. | `test-covered` | design.md#5.2 rule 4 | — | `programs/warden/src/buckets.rs` :: `tests::within_all_caps_ok`<br>`programs/warden/src/buckets.rs` :: `tests::per_tx_exceeded_err`<br>`programs/warden/src/buckets.rs` :: `tests::day_cap_exceeded_err`<br>`programs/warden/tests/transfer.rs` :: `session_sol_over_per_tx_rejected` |
| `WRD-CAP-02` | **Bucket accounting is exact and the ring self-cleans** — spent_today equals the sum of the day's debits, sum(ring) <= per_30d, and no ring slot survives a rollover that should have zeroed it. | `test-covered` | design.md#5.2 rule 4 | — | `programs/warden/src/buckets.rs` :: `tests::day_rolls_over_at_utc_midnight`<br>`programs/warden/src/buckets.rs` :: `tests::ring_30d_cap_enforced_across_days`<br>`programs/warden/src/buckets.rs` :: `tests::ring_zeroes_skipped_days`<br>`programs/warden/src/buckets.rs` :: `tests::ring_slot_index_at_day_29_30_31`<br>`programs/warden/tests/transfer.rs` :: `rolling_30d_cap_enforced_end_to_end` |
| `WRD-CAP-03` | **No cap means zero, never unlimited** — A mint with no cap entry (and a zeroed cap entry) permits outflow of exactly 0; the missing-cap branch rejects rather than skipping the check. | `test-covered` | design.md#5.2 rule 4; #4 (expired/absent caps) | `LZR-ACC-C2` | `programs/warden/src/buckets.rs` :: `tests::unknown_mint_err`<br>`programs/warden/src/buckets.rs` :: `tests::zero_cap_means_no_spend`<br>`programs/warden/src/buckets.rs` :: `tests::find_cap_never_matches_default_mint`<br>`programs/warden/tests/transfer.rs` :: `session_mint_without_account_cap_rejected`<br>`programs/warden/tests/transfer.rs` :: `root_transfer_of_mint_without_threshold_rejected` |
| `WRD-CAP-04` | **An expired or revoked session is exhausted, not absent** — A session past expiry_ts, with a stale generation_at_grant, revoked, or belonging to another account is treated as having zero remaining allowance on every axis, never as an account with no cap configured. | `test-covered` | design.md#4 (exhausted, not absent) | `LZR-ACC-C2`, `SWIG-ACC-C2` | `programs/warden/tests/transfer.rs` :: `session_expired_rejected`<br>`programs/warden/tests/transfer.rs` :: `session_wrong_generation_rejected`<br>`programs/warden/tests/transfer.rs` :: `session_of_another_account_rejected`<br>`programs/warden/tests/transfer.rs` :: `session_mint_without_session_cap_rejected` |
| `WRD-CAP-05` | **Root direct `transfer` is bounded like a session** — The root `transfer` path enforces outflow[mint] <= large_threshold[mint] per transaction AND debits the same account-wide day and rolling-30-day buckets that sessions debit. | `test-covered` | design.md#5.2 rule 4 | — | `programs/warden/tests/transfer.rs` :: `root_transfer_within_threshold_debits_buckets`<br>`programs/warden/tests/transfer.rs` :: `root_transfer_over_threshold_rejected` |
| `WRD-CAP-06` | **Debit is atomic** — A debit that is going to be rejected leaves every bucket field byte-identical to its pre-call value, including when the rejection happens after a day-boundary rollover or an arithmetic overflow guard fires. | `test-covered` | design.md#5.1 (checked arithmetic everywhere) | — | `programs/warden/src/buckets.rs` :: `tests::debit_atomic_on_cap_exceeded_after_day_boundary`<br>`programs/warden/src/buckets.rs` :: `tests::debit_atomic_on_overflow_after_day_boundary`<br>`programs/warden/tests/transfer.rs` :: `token_cpi_failure_leaves_state_unchanged` |
| `WRD-CAP-07` | **Day and 30-day windows are account-wide, not per session** — Every session and the root debit the same SmartAccount.buckets, so N sessions do not get N day caps; and a grant that sets a non-zero per-session per_day/per_30d is rejected rather than stored. | `test-covered` | design.md#4 (what a session cap bounds, exactly) | — | `programs/warden/tests/transfer.rs` :: `two_sessions_share_account_day_cap`<br>`programs/warden/tests/transfer.rs` :: `session_day_cap_across_two_txs`<br>`programs/warden/tests/sessions.rs` :: `grant_with_session_day_caps_rejected` |
| `WRD-CAP-08` | **Session lifetime cap is exact and survives a regrant** — lifetime_spent + amount <= lifetime_cap on every session spend, a regrant merges by mint while preserving spent, and a regrant that would set a lifetime cap below what has already been spent is rejected. | `test-covered` | design.md#4; #5.1 grant_session | `LZR-ACC-H1` | `programs/warden/tests/transfer.rs` :: `session_lifetime_cap_enforced`<br>`programs/warden/tests/sessions.rs` :: `regrant_lower_lifetime_than_spent_rejected`<br>`programs/warden/tests/sessions.rs` :: `regrant_merges_by_mint_and_preserves_spent`<br>`programs/warden/tests/sessions.rs` :: `regrant_cannot_silently_retain_caps_the_signer_never_saw` |
| `WRD-SESS-01` | **ops_mask is bounded by the ceiling AND by the assigned bits** — A grant is rejected unless ops_mask is a subset of policy.session_ops_ceiling AND a subset of the bits this program version has actually assigned; an unassigned bit is refused rather than stored, and an operation whose bit is unset is rejected at use. | `test-covered` | design.md#5.1 grant_session | `LZR-ACC-C2` | `programs/warden/tests/sessions.rs` :: `grant_ops_over_ceiling_rejected`<br>`programs/warden/tests/sessions.rs` :: `grant_with_an_unassigned_ops_mask_bit_rejected`<br>`programs/warden/tests/transfer.rs` :: `session_without_transfer_op_rejected` |
| `WRD-SESS-02` | **Session caps may not exceed the policy ceiling** — Every per-mint cap in a grant is <= policy.session_ceiling for that mint, a mint with no session ceiling cannot be granted at all, and the ceiling itself cannot be set above the account cap at creation. | `test-covered` | design.md#5.1 grant_session | `LZR-ACC-C2` | `programs/warden/tests/sessions.rs` :: `grant_over_ceiling_rejected`<br>`programs/warden/tests/sessions.rs` :: `grant_at_exactly_the_ceiling_accepted`<br>`programs/warden/tests/sessions.rs` :: `grant_mint_without_session_ceiling_rejected`<br>`programs/warden/tests/create_account.rs` :: `rejects_session_ceiling_above_cap`<br>`programs/warden/tests/create_account.rs` :: `rejects_orphan_ceiling_with_no_caps_at_all` |
| `WRD-SESS-03` | **Session expiry is bounded and must be in the future** — A grant's expiry_ts must be strictly in the future and no later than now + policy.max_session_life; the max_session_life boundary itself is accepted, and a policy setting max_session_life above 30 days is refused at creation. | `test-covered` | design.md#5.1 grant_session; #4 | — | `programs/warden/tests/sessions.rs` :: `grant_expiry_too_long_rejected`<br>`programs/warden/tests/sessions.rs` :: `grant_expiry_not_in_the_future_rejected`<br>`programs/warden/tests/sessions.rs` :: `grant_expiry_at_the_max_life_boundary_accepted`<br>`programs/warden/tests/create_account.rs` :: `rejects_max_session_life_above_30_days` |
| `WRD-SESS-04` | **generation_at_grant is refreshed at grant and matched at use** — A grant records the account's current generation, a regrant refreshes it, and a session whose generation_at_grant does not equal the account's current generation cannot act. | `test-covered` | design.md#4; #5.1 grant_session | — | `programs/warden/tests/sessions.rs` :: `regrant_refreshes_generation_at_grant`<br>`programs/warden/tests/sessions.rs` :: `revoke_close_then_regrant_gets_current_generation`<br>`programs/warden/tests/transfer.rs` :: `session_wrong_generation_rejected` |
| `WRD-SESS-05` | **A regrant binds the authority it produces, not only the fields it names** — A merging grant carries prior_authority_hash -- the hash of the retained identity, every cap slot and every lifetime cap it is allowed to merge into -- a mismatch is refused, a fresh grant must carry the zero sentinel, and a regrant must not carry it. | `test-covered` | design.md#5.1 grant_session | `LZR-ACC-H1`, `LZR-ACC-M2` | `programs/warden/tests/sessions.rs` :: `regrant_with_the_true_prior_hash_is_accepted`<br>`programs/warden/tests/sessions.rs` :: `fresh_grant_with_a_non_zero_prior_hash_rejected`<br>`programs/warden/tests/sessions.rs` :: `regrant_with_the_fresh_sentinel_rejected`<br>`programs/warden/tests/sessions.rs` :: `regrant_cannot_silently_retain_caps_the_signer_never_saw`<br>`programs/warden/tests/sessions.rs` :: `grant_body_tamper_rejected_field_by_field` |
| `WRD-SESS-06` | **Only the root or the session itself may revoke it** — revoke_session succeeds for a root ceremony over that exact session and for the session key signing for itself, and fails for a stranger, for a root ceremony naming a different session account, and for a substituted rent-refund destination. | `test-covered` | design.md#5.1 revoke_session | `LZR-ACC-C2`, `LZR-ACC-H1` | `programs/warden/tests/sessions.rs` :: `revoke_by_root_ok`<br>`programs/warden/tests/sessions.rs` :: `revoke_by_session_self_ok`<br>`programs/warden/tests/sessions.rs` :: `revoke_by_stranger_rejected`<br>`programs/warden/tests/sessions.rs` :: `revoke_by_root_wrong_session_account_rejected`<br>`programs/warden/tests/sessions.rs` :: `revoke_by_root_substituted_refund_rejected` |
| `WRD-SESS-08` | **Grant shape limits are enforced, not assumed** — A grant carries at most MAX_CAPS_PER_GRANT = 2 caps, no duplicate mint, matching cap and lifetime-cap lengths, and an Ed25519 session key kind; a 2-cap grant transaction fits the 1232-byte packet. | `test-covered` | design.md#5.1 grant_session (Phase 1A measured limits) | — | `programs/warden/tests/sessions.rs` :: `grant_with_3_caps_rejected`<br>`programs/warden/tests/sessions.rs` :: `duplicate_mint_in_grant_rejected`<br>`programs/warden/tests/sessions.rs` :: `grant_mismatched_cap_and_lifetime_lengths_rejected`<br>`programs/warden/tests/sessions.rs` :: `grant_non_ed25519_kind_rejected`<br>`programs/warden/tests/sessions.rs` :: `grant_tx_fits_1232_bytes_with_2_caps` |
| `WRD-FRZ-01` | **`frozen` gates every 1A outflow and every grant** — While SmartAccount.frozen is set, no Phase 1A instruction that moves value or widens authority succeeds: `transfer` on both the session and the root path, and `grant_session`. | `test-covered` | design.md#5.1 (cross-cutting) | — | `programs/warden/tests/transfer.rs` :: `frozen_blocks_transfer`<br>`programs/warden/tests/transfer.rs` :: `root_freeze_blocks_transfer_and_grant`<br>`programs/warden/tests/freeze.rs` :: `grant_frozen_rejected` |
| `WRD-FRZ-02` | **A freeze is not a brick** — rotate_nonce still succeeds while frozen, so the root can always prove liveness and reach unfreeze; freeze blocks value movement, not the root ceremony itself. | `test-covered` | design.md#5.1 freeze/unfreeze | `ARGENT-ZERO-GUARDIAN` | `programs/warden/tests/freeze.rs` :: `rotate_nonce_still_allowed_when_frozen`<br>`programs/warden/tests/freeze.rs` :: `unfreeze_before_timelock_rejected`<br>`programs/warden/tests/freeze.rs` :: `unfreeze_after_timelock_ok` |
| `WRD-ROOT-03` | **User verification is mandatory** — authenticatorData must have both UP and UV set; a UP-only assertion is rejected. | `test-covered` | design.md#4 | — | `programs/warden/tests/root_verify.rs` :: `up_only_rejected` |
| `WRD-ROOT-04` | **rpIdHash is SHA-256 of the FULL origin, read from state** — rp_id_hash equals SHA-256("chrome-extension://<id>") -- not SHA-256("<id>") -- it is recomputed on-chain from the stored origin at creation, read from SmartAccount state on every verification, and never taken from caller-supplied instruction data or a compiled-in literal. | `test-covered` | design.md#4 (rpIdHash) | `LZR-ACC-H2` | `programs/warden/tests/create_account.rs` :: `rejects_rp_id_hash_not_sha256_of_origin`<br>`programs/warden/tests/create_account.rs` :: `rejects_rp_id_hash_of_bare_extension_id`<br>`programs/warden/tests/root_verify.rs` :: `wrong_rp_id_hash_rejected`<br>`programs/warden/tests/root_verify.rs` :: `rp_id_hash_of_bare_extension_id_rejected` |
| `WRD-ROOT-05` | **Precompile binding and ordering** — The root path binds exactly one secp256r1 precompile instruction, named by index in its own instruction data, at a lower index than the warden instruction, executed by the real secp256r1 program id, carrying exactly one signature whose (pubkey, message, signature) equal (root.pubkey, authenticatorData \|\| SHA256(clientDataJSON), sig). | `test-covered` | design.md#4 | `LZR-ACC-C1` | `programs/warden/tests/root_verify.rs` :: `hand_built_precompile_ix_matches_crate`<br>`programs/warden/tests/root_verify.rs` :: `two_signature_precompile_rejected`<br>`programs/warden/tests/root_verify.rs` :: `foreign_program_at_named_index_rejected`<br>`programs/warden/tests/root_verify.rs` :: `precompile_after_our_ix_rejected`<br>`programs/warden/tests/root_verify.rs` :: `precompile_ix_index_out_of_range_rejected`<br>`programs/warden/tests/root_verify.rs` :: `two_precompile_ixs_only_named_one_binds`<br>`programs/warden/tests/root_verify.rs` :: `message_mismatch_rejected`<br>`programs/warden/tests/root_verify.rs` :: `foreign_ix_index_rejected`<br>`programs/warden/tests/sigverify_wiring.rs` :: `valid_signature_transaction_succeeds`<br>`programs/warden/tests/sigverify_wiring.rs` :: `forged_signature_rejected_by_precompile`<br>`programs/warden/tests/sigverify_wiring.rs` :: `flipped_bit_signature_rejected_by_precompile`<br>`programs/warden/tests/sigverify_wiring.rs` :: `high_s_signature_rejected_by_precompile`<br>`programs/warden/tests/sigverify_wiring.rs` :: `fee_payer_identity_neither_authorizes_nor_blocks` |
| `WRD-ROOT-06` | **Strict depth-0 clientDataJSON scanner** — clientDataJSON is rejected above MAX_CLIENT_DATA_LEN (512 B); the object is walked at depth 0 only; exactly one top-level type/challenge/origin is required and any duplicate top-level key rejects; JSON escapes are decoded before comparison or the value is rejected; type == "webauthn.get"; origin equals the stored origin; challenge equals base64url(transcript hash); a present crossOrigin must be the JSON boolean false. Byte-substring matching is forbidden. | `test-covered` | design.md#4 (strict top-level scanner) | `LZR-ACC-H2` | `programs/warden/src/root_verify/client_data.rs` :: `tests::nested_string_containing_braces_does_not_desynchronise_the_scanner`<br>`programs/warden/src/root_verify/client_data.rs` :: `tests::rejects_duplicate_origin_written_with_unicode_escape`<br>`programs/warden/src/root_verify/client_data.rs` :: `tests::rejects_surrogate_escape`<br>`programs/warden/src/root_verify/client_data.rs` :: `tests::rejects_over_512_bytes`<br>`programs/warden/tests/root_verify.rs` :: `nested_origin_rejected_on_chain`<br>`programs/warden/tests/root_verify.rs` :: `duplicate_origin_rejected_on_chain`<br>`programs/warden/tests/root_verify.rs` :: `cross_origin_true_rejected_on_chain`<br>`programs/warden/tests/root_verify.rs` :: `webauthn_create_type_rejected_on_chain`<br>`programs/warden/tests/root_verify.rs` :: `escaped_origin_accepted_on_chain` |
| `WRD-ROOT-07` | **cluster_tag domain separation** — cluster_tag is a 32-byte client-attested domain separator stored at creation, non-zero-checked, immutable in 1A, and bound into the transcript so an assertion produced against one stored tag cannot be replayed against an account holding a different tag. | `test-covered` | design.md#4 | `LZR-ACC-M2` | `programs/warden/tests/root_verify.rs` :: `wrong_cluster_tag_rejected`<br>`programs/warden/tests/create_account.rs` :: `rejects_zero_cluster_tag` |
| `WRD-ROOT-08` | **Generation-bump semantics (helper-driven)** — A transcript carrying a generation other than the account's current generation is rejected, and a session's authority is scoped to the generation it was granted under. | `test-covered` | design.md#4; #5.1 execute_recovery | — | `programs/warden/tests/root_verify.rs` :: `stale_generation_rejected`<br>`programs/warden/tests/sessions.rs` :: `revoke_close_then_regrant_gets_current_generation` |
| `WRD-ROOT-09` | **action_hash binds the complete semantic inputs** — action_hash = Keccak256(op_type:u8 \|\| borsh(op_args)) where op_args contains every semantic input of the instruction the root authorises; the program recomputes it from the instruction it is executing and rejects on mismatch, so no argument or account can be swapped after the passkey ceremony. | `test-covered` | design.md#4 (action_hash) | `LZR-ACC-M2`, `LZR-ACC-H1` | `programs/warden/src/root_verify/transcript.rs` :: `tests::action_hash_separates_op_type_from_args`<br>`programs/warden/tests/transfer.rs` :: `root_transfer_with_substituted_destination_rejected`<br>`programs/warden/tests/transfer.rs` :: `root_transfer_amount_tamper_rejected`<br>`programs/warden/tests/sessions.rs` :: `grant_body_tamper_rejected_field_by_field`<br>`programs/warden/tests/sessions.rs` :: `revoke_by_root_substituted_refund_rejected`<br>`programs/warden/tests/freeze.rs` :: `op_bytes_are_pinned` |
| `WRD-ROOT-10` | **Transcript preimage byte layout is pinned across the language boundary** — transcript_hash = Keccak256("WARDEN/root/v1" \|\| cluster_tag[32] \|\| program_id[32] \|\| account[32] \|\| generation:u64LE \|\| policy_version:u32LE \|\| root_nonce:u64LE \|\| expiry_ts:i64LE \|\| signed_slot:u64LE \|\| action_hash[32]), and the Rust program, the packages/core TS mirror and the IDL agree on it byte for byte. | `llm-asserted` | design.md#4 (canonical preimage) | `LZR-ACC-H2` | `programs/warden/src/root_verify/transcript.rs` :: `tests::transcript_hash_matches_pinned_vector`<br>`programs/warden/src/root_verify/transcript.rs` :: `tests::transcript_hash_matches_pinned_max_slot_vector`<br>`programs/warden/src/root_verify/transcript.rs` :: `tests::signed_slot_and_expiry_ts_are_not_interchangeable`<br>`programs/warden/src/root_verify/transcript.rs` :: `tests::every_transcript_field_changes_the_hash`<br>`packages/core/test/transcript.test.ts` :: `matches the Rust-pinned vector (transcript_hash_matches_pinned_vector)`<br>`packages/core/test/transcript.test.ts` :: `does not treat expiryTs and signedSlot as interchangeable` |
| `WRD-BUF-03` | **Every root-ceremony transaction fits the 1232-byte packet** — A complete root ceremony transaction (secp256r1 precompile instruction + the warden instruction) serialises to at most MAX_TX_BYTES = 1232 for freeze, unfreeze, a 2-cap grant_session, and root transfer of SOL and of an SPL token. | `test-covered` | design.md#5.1 (Phase 1A measured limits) | — | `programs/warden/tests/freeze.rs` :: `freeze_tx_fits_1232_bytes`<br>`programs/warden/tests/freeze.rs` :: `unfreeze_tx_fits_1232_bytes`<br>`programs/warden/tests/sessions.rs` :: `grant_tx_fits_1232_bytes_with_2_caps`<br>`programs/warden/tests/transfer.rs` :: `root_transfer_tx_fits_1232_bytes`<br>`programs/warden/tests/transfer.rs` :: `root_spl_transfer_tx_fits_1232_bytes` |
| `WRD-BUF-04` | **Every session transaction fits the 1232-byte packet** — A session-signed transfer of SOL and of an SPL token each serialise to at most MAX_TX_BYTES = 1232. | `test-covered` | design.md#5.1 | — | `programs/warden/tests/transfer.rs` :: `session_sol_transfer_tx_fits_1232_bytes`<br>`programs/warden/tests/transfer.rs` :: `session_spl_transfer_tx_fits_1232_bytes` |
| `WRD-BUF-05` | **create_account packet limits are measured, not assumed** — A create_account transaction carrying its mandatory proof-of-possession ceremony fits the 1232-byte packet at MAX_MINTS_AT_CREATE = 1 mint (cap + ceiling + threshold) and does NOT fit at 2, and more than MAX_MINTS_AT_CREATE mints is rejected on-chain. | `test-covered` | design.md#5.1 create_account | — | `programs/warden/tests/create_pop.rs` :: `create_with_pop_transaction_sizes_are_measured_and_bounded`<br>`programs/warden/tests/create_account.rs` :: `realistic_policy_transaction_fits_the_packet_limit`<br>`programs/warden/tests/create_account.rs` :: `realistic_policy_plus_one_mint_does_not_fit`<br>`programs/warden/tests/create_account.rs` :: `max_mints_at_create_transaction_fits_the_packet_limit`<br>`programs/warden/tests/create_account.rs` :: `full_max_mint_caps_policy_does_not_fit_the_packet_limit`<br>`programs/warden/tests/create_account.rs` :: `rejects_more_mints_than_max_mints_at_create` |

### Phase 1B

| ID | Invariant | Status | Spec | Prior art | Evidence |
|---|---|---|---|---|---|
| `WRD-NONCE-03` | **Slot-based root freshness** — A root ceremony is accepted only while signed_slot <= Clock::slot < signed_slot + MAX_ROOT_SLOT_AGE (150): a future slot is rejected outright and an age of exactly 150 slots is rejected. | `test-covered` | design.md#4 (freshness is slot-based, rev 8) | `LZR-ACC-M1` | `programs/warden/tests/root_verify.rs` :: `slot_age_zero_accepted`<br>`programs/warden/tests/root_verify.rs` :: `slot_age_149_accepted`<br>`programs/warden/tests/root_verify.rs` :: `slot_age_150_rejected_as_stale`<br>`programs/warden/tests/root_verify.rs` :: `future_slot_by_one_rejected`<br>`programs/warden/tests/root_verify.rs` :: `signed_slot_u64_max_rejected_as_future_not_overflow`<br>`programs/warden/tests/root_verify.rs` :: `signed_slot_edited_after_signing_rejected`<br>`programs/warden/tests/root_verify.rs` :: `valid_expiry_does_not_rescue_a_stale_slot`<br>`programs/warden/tests/root_verify.rs` :: `fresh_slot_does_not_rescue_an_expired_ceremony`<br>`programs/warden/tests/root_verify.rs` :: `phase_1a_root_args_without_signed_slot_fails_to_decode` |
| `WRD-CAP-09` | **Root `execute` and `swap` are bounded like a session** — The root `execute` and `swap` paths enforce outflow[mint] <= large_threshold[mint] per transaction AND debit the same account-wide day and rolling-30-day buckets; anything larger must come from execute_pending, whose execution also debits the buckets. | `unimplemented` | design.md#5.2 rule 4 | — | — |
| `WRD-SESS-07` | **A non-zero program_allowlist_id must name an allocated registry list** — A grant carrying a non-zero program_allowlist_id is rejected (InvalidAllowlistId) unless the account is bound to a Registry AND the id names an ALLOCATED list (>=1 live entry) in that Registry, loaded and checked at grant time — so no session can hold a dangling reference to an unallocated, out-of-range, or unreferenced list. Structural range validity (is_valid_list_id) is necessary but not sufficient; allocation (is_allocated_list) is the binding gate. | `test-covered` | design.md#5.1 grant_session | — | `programs/warden/tests/sessions.rs` :: `grant_with_unallocated_allowlist_id_rejected`<br>`programs/warden/tests/sessions.rs` :: `grant_with_allocated_allowlist_id_accepted`<br>`programs/warden/tests/sessions.rs` :: `grant_with_unknown_allowlist_id_rejected` |
| `WRD-EXEC-01` | **Field-identity conservation on vault-owned token accounts** — For every account whose BEFORE snapshot is a vault-owned token account, every field except amount is byte-identical AFTER (runtime owner, token owner, mint, delegate, delegated_amount, close_authority, state, is_native, data_len, TLV-tail hash) and the after-state independently satisfies policy (state == Initialized, delegate == None, close_authority == None). | `unimplemented` | design.md#5.2 rule 2 | `SWIG-ACC-C1`, `LZR-ACC-C1` | — |
| `WRD-EXEC-02` | **Outflow is coalesced per mint before a single debit** — outflow[mint] is summed across ALL vault-owned token accounts of that mint (negative = inflow, floored at 0 for cap purposes) and passed to exactly one buckets::debit call per mint per instruction. | `unimplemented` | design.md#5.2 rule 4 | — | — |
| `WRD-EXEC-03` | **The SOL equation counts each lamport exactly once** — outflow[SOL] = (pda_lamports_before - pda_lamports_after) + sum over vault NATIVE accounts of (account_lamports_before - account_lamports_after); a native account's value is its LAMPORTS, not its cached `amount` (which only moves on SyncNative); native lamports are counted here and are exempt from the lamports-unchanged rule; non-native CloseAccount rent enters only via the decoder's decoded close intents (spec 5.2 rule 4a). | `unimplemented` | design.md#5.2 rules 4 and 4a | — | — |
| `WRD-EXEC-04` | **Mint authority fields are byte-identical** — Whenever a vault-owned token account of mint M is writable, M's mint account must be present; if the vault holds any authority on M, every authority field (mint_authority, freeze_authority, and for Token-2022 transfer_fee_config_authority / withdraw_withheld_authority, extracted by TLV type and offset) is compared byte-for-byte and any change is an instruction reject, not an accounting entry. | `unimplemented` | design.md#5.2 rule 2a | `SWIG-ACC-C1` | — |
| `WRD-EXEC-05` | **A vault-owned token account may not disappear** — A vault-owned token account present BEFORE that does not exist or does not parse AFTER is a reject, unless the payload decoder handed the comparison a matching decoded close intent for that exact account; nothing downstream may infer a close from a disappearance. | `unimplemented` | design.md#5.2 rules 1a, 2, 3 | `SWIG-ACC-C1` | — |
| `WRD-EXEC-06` | **accounts_hash binds the logical list** — accounts_hash is computed over the LOGICAL account list in logical order (logical[0]=smart_account, logical[1]=signer, logical[2+k]=remaining_accounts[k]), hashing pubkey \|\| is_signer \|\| is_writable for each; no payload index ever refers to a physical slot and no code indexes the raw account slice. | `test-covered` | design.md#5.2 (execute payload contract) | `LZR-ACC-C1`, `LZR-ACC-H1` | `programs/warden/src/payload.rs` :: `accounts_hash_is_order_and_flag_sensitive`<br>`programs/warden/tests/execute.rs` :: `lazorkit_account_reorder_under_captured_assertion_rejected`<br>`programs/warden/tests/execute.rs` :: `execute_signer_aliased_in_remaining_rejected`<br>`programs/warden/tests/execute.rs` :: `execute_pda_aliased_in_remaining_rejected` |
| `WRD-EXEC-07` | **No inner instruction may set a delegate on a vault-owned token account** — No DIRECT inner instruction of execute may set, change or clear a delegate on a vault-owned token account (the decoder deny-list refuses Approve/ApproveChecked/Revoke on both paths), and no CPI — direct or nested — may leave a DURABLE delegate change: delegate and delegated_amount must be byte-identical before vs after AND None/0 in both snapshots. A transient same-CPI set-use-clear inside a forwarding program's own execution restores field identity and is NOT observable by any before/after comparison — that case is the accepted spec §5.3 external-program boundary, bounded by the net per-mint conservation caps, and product claims must not state the absolute. | `unimplemented` | design.md#5.2 rules 1a and 3 | `SWIG-ACC-C1`, `LZR-ACC-C2` | — |
| `WRD-EXEC-08` | **Program owner is compared, not just field values** — The before/after snapshot compares each account's runtime program owner, so an account that is transferred out, closed and reopened under a program that fakes the SPL token byte layout fails the comparison. | `unimplemented` | design.md#5.2 rules 2 and 3 | `SWIG-ACC-C1` | — |
| `WRD-EXEC-09` | **Token-2022 danger extensions reject unconditionally in 1B** — permanent_delegate (12), transfer_hook (14) and confidential-transfer mints (4/5) reject unconditionally; transfer_fee (1) also rejects in 1B because the allow-list machinery is 1C. The decision is made by inspecting the mint's TLV, which is why the mint account must be present. | `unimplemented` | design.md#5.2 rule 5 | — | — |
| `WRD-EXEC-10` | **Adapter-decoded max_in and a pinned source ATA bound the call** — Before any inner CPI runs, the adapter parses max_in out of the inner instruction's own data (never inferred from balances) and compares it against the session per_tx and the account buckets, and the adapter's role validator requires the inner instruction's source account to be the specific vault ATA the caller declared. | `unimplemented` | design.md#5.2 rule 4b; #5.3 | — | — |
| `WRD-EXEC-11` | **SDK wrap reproduces the handler's logical list and privileges, or fails closed** — The client's wrapForExecute builds the COMPLETE outer execute instruction (named optionals + the real fee payer + remaining), compiles and decompiles it, and derives the accounts_hash from the resulting EFFECTIVE (runtime-coalesced) instruction metas — one source of truth, matching what the handler hashes from runtime AccountInfo. Every privilege a payload reference requests is a SUBSET of the effective outer logical meta (the PDA's invoke_signed signature excepted), enforced by a client guard. Any shape the ABI cannot honor is REJECTED, never silently rewritten: a third-party signer, a writable SmartAccount PDA, or a writable root/session signer (all fail-closed). A writable idx-0 is expressible only through the raw encodeExecutePayload codec (cross-language fixture); a typed vault-sweep CloseAccount builder is a deferred client deliverable, not implemented. | `test-covered` | design.md#5.2 (execute payload contract) | `LZR-ACC-C1`, `LZR-ACC-H2` | `packages/core/src/execute/wrap.ts`<br>`packages/core/test/wrap.test.ts` :: `hashes logical[1] writable when signer is the payer, read-only for a separate relayer`<br>`packages/core/test/wrap.test.ts` :: `hashes logical[1] writable when a writable named optional aliases the signer`<br>`packages/core/test/wrap.test.ts` :: `rejects an inner instruction that requires a third-party signer`<br>`packages/core/test/wrap.test.ts` :: `rejects an inner instruction that names the SmartAccount PDA writable`<br>`packages/core/test/wrap.test.ts` :: `rejects an inner instruction that names the root/session signer writable`<br>`packages/core/test/wrap.test.ts` :: `holds the payload⊆outer privilege-subset invariant on a normal shape`<br>`programs/warden/src/payload.rs` :: `ts_coalesced_logical_hash_matches`<br>`packages/core/test/wrap.test.ts` :: `reproduces the committed coalesced-logical golden and its pinned structure`<br>`packages/core/test/wrap.test.ts` :: `generate() emits exactly the five goldens through the injected sink, no disk write`<br>`packages/core/test/wrap.test.ts` :: `the gen-fixtures runner does not generate when imported (main-module check false)` |
| `WRD-EXEC-12` | **SDK wrap satisfies the execute liveness budget** — The client's wrapForExecute produces a transaction that meets the on-chain execute liveness budget: ComputeBudget instructions are hoisted TOP-LEVEL; the SetComputeUnitLimit is strictly decoded and validated against a measured floor and Solana's ceiling; a RequestHeapFrame is ALWAYS attached, sized from the real heap drivers (inner-instruction count, account references, payload bytes) and clamped to [128 KiB floor, 256 KiB ceiling] as a multiple of 1024, and a caller-supplied frame is preserved ONLY if it clears the runtime's alignment + range rules and covers the shape; and the on-chain account caps are enforced client-side from the effective post-coalescing privileges so an over-cap shape fails fast. | `test-covered` | design.md#5.2 (execute payload contract) | — | `packages/core/src/execute/wrap.ts`<br>`packages/core/test/wrap.test.ts` :: `always attaches a RequestHeapFrame at or above the measured floor`<br>`packages/core/test/wrap.test.ts` :: `keeps a dApp price setting AND still injects a validated compute-unit limit`<br>`packages/core/test/wrap.test.ts` :: `rejects a dApp compute-unit limit below the measured floor`<br>`packages/core/test/wrap.test.ts` :: `rejects a configured default limit below the floor`<br>`packages/core/test/wrap.test.ts` :: `validates a caller-supplied heap frame against the runtime range and alignment`<br>`packages/core/test/wrap.test.ts` :: `counts a payer-promoted remaining account against the writable cap`<br>`packages/core/test/wrap.test.ts` :: `rejects a shape past the on-chain account cap` |
| `WRD-FRZ-03` | **1B value paths refuse while frozen** — execute, swap and execute_pending each refuse while frozen, re-validated against CURRENT state at execution rather than at queue time. | `unimplemented` | design.md#5.1 execute_pending | — | — |
| `WRD-ROOT-01` | **Proof of possession at account creation** — create_account cannot succeed without a valid root ceremony over the derived address, and the address itself is Keccak256("WARDEN/seed/v1" \|\| root_pubkey33 \|\| salt32) computed on-chain, so the PDA cannot be squatted with an attacker-controlled root: a different root derives a different address, and the victim's address cannot be reached without the victim's passkey. | `test-covered` | design.md#5.1 create_account | `TOB-SQUADS-7` | `programs/warden/tests/create_pop.rs` :: `squat_race_attacker_cannot_reach_the_victims_address`<br>`programs/warden/tests/create_pop.rs` :: `create_without_a_ceremony_is_rejected`<br>`programs/warden/tests/create_pop.rs` :: `create_with_an_assertion_by_the_wrong_passkey_is_rejected`<br>`programs/warden/tests/create_pop.rs` :: `a_substituted_salt_is_a_challenge_mismatch`<br>`programs/warden/tests/create_pop.rs` :: `the_creating_assertion_cannot_be_replayed_for_a_second_salt`<br>`programs/warden/tests/create_pop.rs` :: `off_curve_root_cannot_be_created_because_no_assertion_verifies`<br>`programs/warden/tests/create_pop.rs` :: `create_through_a_middleman_cpi_is_rejected` |
| `WRD-ROOT-02` | **Root instructions are transaction-level only** — Every root instruction requires get_stack_height() == TRANSACTION_LEVEL_STACK_HEIGHT, so a root path can never be invoked through a middleman program. | `test-covered` | design.md#5.1 (cross-cutting) | — | `programs/warden/tests/root_verify.rs` :: `root_instruction_via_cpi_rejected` |
| `WRD-DENY-01` | **Fixed deny-list, on both paths, above the registry** — SetAuthority, Approve, ApproveChecked and Revoke on SPL Token and Token-2022 are rejected in the execute payload decoder on BOTH the session and the root path, before dispatch and before any registry lookup; CloseAccount is rejected unless ALL of: it is a direct payload instruction (not nested inside another program's CPI), the account's amount_before is exactly 0, the decoded destination is the SmartAccount PDA, and the account is neither the PDA itself nor a Mint. | `unimplemented` | design.md#5.2 rule 1a | `SWIG-ACC-C2`, `LZR-ACC-C2` | — |
| `WRD-DENY-02` | **No registry entry can re-enable a denied pair** — A registry (or policy) list containing a denied (program, instruction) pair still fails closed: the decoder runs first, there is no override flag, and the deny-list is unreachable by the section 5.4 loosening lattice. | `unimplemented` | design.md#5.2 rule 1a; #5.4 | `LZR-ACC-C2` | — |
| `WRD-BUF-01` | **A squatted Stage is recoverable** — A stranger who pre-computes a victim's staged content cannot block or squat the victim's Stage: the PDA binds the creator (['stage', account, creator, hash]), so the stranger's stage and the victim's are different addresses and the victim's stage_open always succeeds — the ND-SQD3-LO-01 creator-less-seed squat is closed by construction, not merely time-boxed. | `test-covered` | design.md#5.1 stage_open/stage_close | `ND-SQD3-LO-01`, `CERTORA-H-01`, `TOB-SQUADS-7` | `programs/warden/tests/stage.rs` :: `stranger_cannot_occupy_the_victims_stage_address`<br>`programs/warden/tests/stage.rs` :: `two_creators_stage_same_content_at_distinct_addresses`<br>`programs/warden/tests/stage.rs` :: `stranger_cannot_chunk_or_early_close_victims_stage`<br>`programs/warden/tests/stage.rs` :: `open_rejects_expiry_beyond_max_ttl` |
| `WRD-BUF-02` | **No unbounded uncleanable buffer** — There is no per-account fixed slot table an attacker can exhaust (each (account, creator, content) is its own PDA), and every Stage — finalized or not — has a permissionless closure path after expiry_ts that returns the rent to its creator (Certora H-01 lifecycle GC). | `test-covered` | design.md#5.1 stage_* | `ND-SQD3-LO-01`, `CERTORA-H-01` | `programs/warden/tests/stage.rs` :: `unfinalized_stage_closed_by_anyone_after_expiry`<br>`programs/warden/tests/stage.rs` :: `finalized_stage_closed_by_anyone_after_expiry`<br>`programs/warden/tests/stage.rs` :: `creator_closes_before_finalize_and_gets_rent` |
| `WRD-STAGE-01` | **stage_finalize checks the content hash and records generation + policy_version** — stage_finalize requires written == len and Keccak256(data) == hash (the content-address is verified, not assumed), and records the bound SmartAccount's live generation and policy_version — the account PDA re-derived from its stored seed so a planted copy cannot supply a forged generation. The execute-time consume/current-state check is WRD-STAGE-02. | `test-covered` | design.md#5.2 rule 6 | `TOB-SQUADS-8` | `programs/warden/tests/stage.rs` :: `open_chunk_finalize_records_and_reads_back`<br>`programs/warden/tests/stage.rs` :: `finalize_with_wrong_content_rejected`<br>`programs/warden/tests/stage.rs` :: `finalize_incomplete_rejected`<br>`programs/warden/tests/stage.rs` :: `finalize_rejects_noncanonical_smart_account` |
| `WRD-STAGE-02` | **Staged content is single-use and bound to current account state at execution** — execute (root or session) accepts a finalized Stage only when its recorded generation and policy_version still equal the account's current values, verifies Keccak256(stage.data) == the signed payload_hash, and closes the Stage on success so it cannot be replayed; a failed execute reverts, leaving the Stage and the root nonce intact for retry. | `unimplemented` | design.md#5.3 | — | — |
| `WRD-CONS-01` | **Unit layer: per-mint net outflow, coalesced and floored** — compare_and_account returns outflow per mint computed from before/after snapshots: multiple vault ATAs of one mint coalesce into a single entry, intra-vault rebalances net to zero, and inflow floors to zero rather than crediting a negative outflow. | `test-covered` | design.md#5.2 rules 3/4 | — | `programs/warden/src/conservation/tests.rs` :: `amount_decrease_becomes_outflow`<br>`programs/warden/src/conservation/tests.rs` :: `two_vault_atas_of_one_mint_coalesce_into_a_single_entry`<br>`programs/warden/src/conservation/tests.rs` :: `intra_vault_rebalance_nets_to_zero`<br>`programs/warden/src/conservation/tests.rs` :: `inflow_floors_to_zero_and_drops_the_mint_entry` |
| `WRD-CONS-02` | **Unit layer: one SOL equation across WSOL and PDA lamports** — Wrapped-SOL value is measured by LAMPORTS, not the cached `amount`, and is decided by `is_native` (which must agree with the native mint key or the account is rejected); wrapped-SOL and PDA lamport deltas merge into one SOL number, floored once, and a native mint key never appears in by_mint. | `test-covered` | design.md#5.2 rule 4a | — | `programs/warden/src/conservation/tests.rs` :: `wsol_decrease_is_counted_in_sol_and_never_in_by_mint`<br>`programs/warden/src/conservation/tests.rs` :: `wsol_and_pda_lamport_delta_merge_into_one_sol_number`<br>`programs/warden/src/conservation/tests.rs` :: `a_pda_inflow_offsets_a_wsol_outflow_before_the_floor`<br>`programs/warden/src/conservation/tests.rs` :: `t22_native_account_delta_is_counted_in_sol_not_by_mint`<br>`programs/warden/src/conservation/tests.rs` :: `a_native_lamport_drain_with_unchanged_amount_is_sol_outflow`<br>`programs/warden/src/conservation/tests.rs` :: `sync_native_then_transfer_of_a_donation_is_counted_as_sol_outflow`<br>`programs/warden/src/conservation/tests.rs` :: `an_is_native_flag_on_a_non_native_mint_is_rejected` |
| `WRD-CONS-03` | **Unit layer: authority or identity mutation is a reject, never an accounting entry** — Any change to a vault token account's owner, delegate, close authority, state, is_native, data length, TLV tail, runtime program owner, or backing-mint identity/authorities is InstructionRejected/ConservationViolated — it is never converted into an outflow number; pre-existing delegates and close authorities reject even when unchanged. | `test-covered` | design.md#5.2 rules 2/2a | `SWIG-ACC-C1` | `programs/warden/src/conservation/tests.rs` :: `delegate_set_by_the_cpi_is_rejected`<br>`programs/warden/src/conservation/tests.rs` :: `token_owner_change_is_rejected`<br>`programs/warden/src/conservation/tests.rs` :: `runtime_program_owner_change_is_rejected`<br>`programs/warden/src/conservation/tests.rs` :: `a_pre_existing_delegate_is_rejected_even_if_unchanged`<br>`programs/warden/src/conservation/tests.rs` :: `mint_authority_change_is_a_reject_not_an_accounting_entry`<br>`programs/warden/src/conservation/tests.rs` :: `data_len_and_tlv_tail_changes_are_rejected`<br>`programs/warden/src/conservation/tests.rs` :: `a_required_mint_with_an_unmodeled_extension_is_rejected`<br>`programs/warden/src/conservation/tests.rs` :: `a_standalone_writable_mint_with_an_unmodeled_extension_is_rejected`<br>`programs/warden/src/conservation/tests.rs` :: `a_standalone_writable_mint_with_a_recognized_danger_extension_is_rejected` |
| `WRD-CONS-04` | **Unit layer: accounts cannot appear, vanish, or move without an explanation** — A vault token account that disappears without a matching CloseIntent, appears vault-owned only in the AFTER snapshot, changes list position, or duplicates another pubkey is rejected; a legal vault-sweep close is counted once and only once. | `test-covered` | design.md#5.2 rules 1a/2/3 | `SWIG-ACC-C1`, `LZR-ACC-C1` | `programs/warden/src/conservation/tests.rs` :: `disappearance_without_a_close_intent_is_rejected`<br>`programs/warden/src/conservation/tests.rs` :: `positional_mismatches_are_rejected`<br>`programs/warden/src/conservation/tests.rs` :: `a_vault_sweep_close_is_legal_and_is_not_double_counted`<br>`programs/warden/src/conservation/tests.rs` :: `a_close_intent_for_an_account_that_survived_is_rejected`<br>`programs/warden/src/conservation/tests.rs` :: `a_close_intent_with_a_non_zero_balance_is_rejected`<br>`programs/warden/src/conservation/tests.rs` :: `c2_an_account_converted_into_a_vault_token_account_is_rejected` |
| `WRD-CONS-05` | **Unit layer: strict, fail-closed SPL / Token-2022 layout parsing** — Classic SPL parses only exactly 82-byte mints and 165-byte token accounts; COption tags other than 0/1 are parse errors; T22 TLV tails are walked bounded and fail closed, danger extensions each set their bit, and transfer-fee authorities are extracted by TLV type rather than hashed with the whole tail. | `test-covered` | design.md#5.2 rules 2a/5 | `LZR-ACC-H2` | `programs/warden/src/conservation/tests.rs` :: `classic_82_byte_mint_parses`<br>`programs/warden/src/conservation/tests.rs` :: `mint_buffer_of_81_or_83_bytes_does_not_parse`<br>`programs/warden/src/conservation/tests.rs` :: `coption_tag_two_is_a_parse_error_never_tag_not_zero`<br>`programs/warden/src/conservation/tests.rs` :: `t22_malformed_tlv_does_not_parse`<br>`programs/warden/src/conservation/tests.rs` :: `transfer_fee_authorities_are_extracted_by_tlv_type`<br>`programs/warden/src/conservation/tests.rs` :: `every_danger_extension_sets_its_bit`<br>`programs/warden/src/conservation/tests.rs` :: `t22_identifier_constants_match_independent_literals` |
| `WRD-CONS-06` | **Unit layer: conservation arithmetic cannot wrap** — Outflow summation uses checked arithmetic and rejects on overflow instead of wrapping; a crafted pair of snapshots whose deltas sum past u64::MAX is an error, not a small outflow. | `test-covered` | design.md#5.2 (checked_* only, repo directive) | `MOONWELL-DIMENSIONAL` | `programs/warden/src/conservation/tests.rs` :: `outflow_overflow_is_rejected` |
| `WRD-EVT-01` | **Structured events on every value/policy transition** — execute/swap/transfer emit a structured event on every successful bucket debit and every cap rejection, and freeze/unfreeze emit freeze events; the notifier counts BucketDebited and freeze-transition events only from a finalized transaction with meta.err == null (a rolled-back debit's surviving log is not a spend), and treats CapRejected/DeniedInstructionRejected as failed-transaction signals. Each event has a replay-safe identity (transaction signature + outer/inner instruction index + event ordinal) so a reconnect/backfill/restart cannot double-count a legitimate repeated debit. | `unimplemented` | design.md#5.2 | — | — |
| `WRD-DEP-01` | **Upgrade authority is the derived Squads vault PDA** — The deployment gate authenticates Warden's canonical ProgramData (via the pinned Program account + BPF loader ownership) and requires its upgrade authority to be the derived canonical vault PDA of a multisig whose COMPLETE audited configuration is pinned and attested from reviewed config: program id, multisig pubkey, member pubkey set AND per-member permission masks, threshold, time-lock, configAuthority == default, no actionable stale/pre-approved proposal state, and the audited Squads code hash; Squads' own upgrade governance is the documented trust-root terminus. The byte-level field list and fixtures are finalized at Task 11R implementation against the pinned Squads IDL. | `test-covered` | design.md#5.5 | — | `packages/core/src/deploy/gate.ts`<br>`packages/core/test/deploy-gate.test.ts` :: `passes the happy path — pinned identity, 3-of-5, fresh, autonomous, matching hashes`<br>`packages/core/test/deploy-gate.test.ts` :: `refuses a pin that weakens the spec-fixed governance shape`<br>`packages/core/test/deploy-gate.test.ts` :: `refuses an unauthenticated / forked cluster (wrong genesis hash)`<br>`packages/core/test/deploy-gate.test.ts` :: `rejects a structurally-perfect 3-of-5 with an ATTACKER member set (round-3 case)`<br>`packages/core/test/deploy-gate.test.ts` :: `rejects a non-default config authority (WRDF-0017 round 4)`<br>`packages/core/test/deploy-gate.test.ts` :: `permits terminal governance history (transaction_index > 0)`<br>`packages/core/test/deploy-gate.test.ts` :: `rejects an inconsistent stale boundary (stale_transaction_index > transaction_index)`<br>`packages/core/test/deploy-gate.test.ts` :: `deriveVaultPda reproduces the independent golden vector (locks seeds/order/index)`<br>`packages/core/test/deploy-gate.test.ts` :: `rejects a non-executable Warden Program account (a data account is not the program)`<br>`packages/core/test/deploy-gate.test.ts` :: `rejects replacement Squads code (hash != pinned audited hash, WRDF-0017 round 6)`<br>`packages/core/test/deploy-gate.test.ts` :: `every named fixture case yields the expected verdict`<br>`packages/core/test/deploy-cli.test.ts` :: `resolveManifest returns a committed pin and rejects an unknown name (WRDF-0085)`<br>`packages/core/test/deploy-cli.test.ts` :: `crossCheckIdentities refuses missing or mismatched identities (WRDF-0085)`<br>`packages/core/test/deploy-cli.test.ts` :: `proposalAuditResult ALWAYS fails closed — no attestation bypass (WRDF-0028)` |
| `WRD-DEP-02` | **Adapter selectors match on-chain Registry at deploy** — The deployment gate authenticates the canonical Registry PDA (owner, discriminator, version) and attests its COMPLETE reviewed configuration against pinned config: every adapter selector re-derived from source (Anchor sighash from the IDL where one exists, per-program instruction tag otherwise) and diffed (missing, extra, wrong, or duplicate selectors and role-validator mismatches reject), plus the allowlist `lists` and the `treasury` fee destination. The byte-level field list and fixtures are the Task 3/deploy-time implementation artifact against the Registry layout. | `unimplemented` | design.md#5.5 | — | — |

### Phase 1C

| ID | Invariant | Status | Spec | Prior art | Evidence |
|---|---|---|---|---|---|
| `WRD-ROOT-11` | **Recovery invalidates everything outstanding** — A successful execute_recovery replaces the root and increments generation, and that increment invalidates every outstanding assertion, session, stage and pending action -- proven end to end through the real instruction, not a test helper. | `unimplemented` | design.md#5.1 execute_recovery | `ARGENT-ZERO-GUARDIAN` | — |

### Phase client

| ID | Invariant | Status | Spec | Prior art | Evidence |
|---|---|---|---|---|---|
| `WRD-KEY-01` | **Root key non-exportable, never in extension memory** — Warden exposes no root-export API and root private-key material never enters extension memory; session and recovery secrets are typed and cannot be used as root authority. (A platform-synced passkey may itself be backup-eligible and movable by the OS/authenticator — that is the T7 threat, outside Warden's enforcement; Warden's boundary is that IT never exports or holds root material.) | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C2 | — | — |
| `WRD-KEY-02` | **No retained password; no equality-check auth** — No plaintext password is retained after derivation; no password equality check substitutes for fresh cryptographic authentication. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C2 | — | — |
| `WRD-KEY-03` | **Absolute wall-clock unlock deadlines** — Idle and hard unlock deadlines are absolute wall-clock values, checked on every key use and after every service-worker wake; expiry clears all session unlock material. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C2 | — | — |
| `WRD-KEY-04` | **Versioned AEAD keyring envelope with contextual AAD** — Encrypted keyring envelopes are versioned AEAD with random nonces and AAD binding account, full extension origin, key kind, schema version, canonical cluster identity (genesis hash), and Warden program id; malformed, unknown-version, replayed-context (incl. cross-cluster), and tampered envelopes reject. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C2 | — | — |
| `WRD-EXT-01` | **Browser-owned message provenance** — Privileged background methods derive origin, extension id, tab, and frame from browser-owned sender metadata, never page-supplied data. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C1 | — | — |
| `WRD-EXT-02` | **Privileged UI port requires proven extension identity** — An extension UI port is privileged only when sender.id == chrome.runtime.id AND browser-owned sender.url/origin is the exact chrome-extension://<id> origin at an allowed extension-page path; extension-id equality alone is insufficient because an extension-owned content script shares sender.id and can call runtime.connect. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C1 | — | — |
| `WRD-APR-01` | **Immutable single-use approval record** — Every approval is an immutable, expiring, single-use background record binding request id, origin, account, method, cluster, serialized message bytes and digest. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C3 | — | — |
| `WRD-APR-02` | **Approval renders exact bytes; signer rechecks digest** — The approval UI renders from those exact bytes and the signer rechecks the digest immediately before signing; caller labels, route params, or stale simulation objects never define the displayed intent. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C3 | — | — |
| `WRD-APR-03` | **Atomic single-winner approval resolution** — Concurrent approve/reject/timeout/navigation events have one atomic winner and cannot sign twice or resolve the wrong request. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C3 | — | — |
| `WRD-TXI-01` | **Local decode of signed bytes; no blind-sign in v1** — Supported instructions are decoded locally from serialized bytes; unknown or ambiguous instructions never receive a benign verdict and there is no blind-sign fallback in v1. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C4 | — | — |
| `WRD-SIM-01` | **Simulation/reputation bound to digest and freshness** — Every simulation/reputation result is bound to exact message digest, signer/account, cluster identity, and freshness; mismatch or absence cannot display a green/safe state. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C4 | — | — |
| `WRD-SIM-02` | **Simulator output is advisory only** — Simulator output is advisory and can neither authorize an on-chain-denied action nor bypass local/on-chain policy. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C4 | — | — |
| `WRD-EXP-01` | **Fresh ceremony for any secret export** — Any recovery-secret reveal or export requires a fresh ceremony; Warden exposes no export path for root or session secrets and root material never enters extension memory; portable recovery envelopes retain the specified 128-bit secret strength and bind AAD to account, genesis hash, Warden program id, recovery-key context, and version (a SmartAccount PDA is not network-qualified, so account-only AAD transplants across clusters). (Platform passkey sync is outside this boundary — see WRD-KEY-01 and threat T7.) | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C5 | — | — |
| `WRD-REL-01` | **Byte-reproducible extension payload** — A clean checkout at a release tag produces a byte-identical extension payload in two isolated builders, with an approved SHA-256. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C6 | — | — |
| `WRD-REL-02` | **Published store payload matches approved artifact** — The published store payload is compared with the approved artifact, and any unexplained file/permission/CSP difference blocks release. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C6 | — | — |
| `WRD-REL-03` | **Two-person least-privilege publisher authority** — Publisher authority is least-privilege, phishing-resistant, recoverable, and two-person controlled; no long-lived publishing secret is committed or printed by CI. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C6 | — | — |
| `WRD-ORG-01` | **Origin/build-ID lifecycle fails closed** — Creation requires the stored origin to equal the permitted production origin and the stored rp_id_hash to equal SHA-256(stored origin); an assertion is accepted only when authenticatorData.rpIdHash equals the stored rp_id_hash AND the parsed clientDataJSON.origin equals the stored origin byte-for-byte. cluster_tag is caller-attested and is not the boundary; the pinned production origin is. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C1a | `TOB-SQUADS-7` | — |
| `WRD-SIG-01` | **Strict-DER parsing and mandatory low-S normalization** — Every browser WebAuthn assertion is parsed with strict DER (trailing bytes, non-minimal lengths, r/s out of range rejected), converted to fixed 64-byte compact form, and unconditionally low-S normalized before submission; a recorded real high-S Chrome assertion passes the on-chain precompile end to end only after normalization. | `unimplemented` | design.md#4 | `LZR-ACC-H2` | — |
| `WRD-QTE-01` | **Swap quote-source independence** — Both the primary and the check quote are bound to one canonical QuoteIntent (approval id + exact message digest, account, cluster, input/output mint pubkeys and decimals, ExactIn/ExactOut mode, base-unit amount, slippage/fee convention); the divergence-checked adverse quantity is net output after fees for ExactIn and total required input after fees for ExactOut, computed with checked integer math; >300 bps divergence blocks unoverridably, a stale primary re-quote that changes the intent invalidates the approval, and a check quote sharing the primary's upstream renders as no independent check, never as agreement. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-client-security-hardening.md#C4b | — | — |

### Phase vanity

| ID | Invariant | Status | Spec | Prior art | Evidence |
|---|---|---|---|---|---|
| `WRD-VAN-01` | **Vanity varies only a public salt, never a signing secret** — Vanity search varies only a 32-byte salt; it never generates or exports a signing secret, and the trusted client independently re-derives a worker result. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-vanity-primary-account.md#V0 | — | — |
| `WRD-VAN-02` | **1-4 ASCII Base58 chars enforced at every layer** — Every layer enforces 1-4 ASCII Base58 characters and exact prefix/suffix plus capitalization semantics; stale or mutated jobs cannot become the selected account. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-vanity-primary-account.md#V0 | — | — |
| `WRD-VAN-03` | **Create ceremony binds selected salt to root/config** — The create ceremony and post-confirmation readback bind the selected salt/address to root, program id, cluster/config, bump, and initial policy before funding or receive UI is enabled. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-vanity-primary-account.md#V0 | — | — |
| `WRD-VAN-04` | **Local, auditable, telemetry-free v1 generation** — v1 generation is local, bundled, source-auditable, and emits no wallet-linkable telemetry or private-key-shaped output. | `unimplemented` | docs/superpowers/plans/2026-08-19-warden-vanity-primary-account.md#V0 | — | — |

<!-- END GENERATED -->
